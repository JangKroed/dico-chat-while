{
  "name": "dico-chat-while",
  "version": "0.2.30",
  "private": true,
  "type": "module",
  "description": "현재 Discord 채널에서 두 공지를 번갈아 예약하는 Chrome 확장 프로그램",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
